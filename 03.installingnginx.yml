- name: Installing the nginx
  hosts: web
  tasks:
  - name: intall nginx
    ansible.builtin.package:
      name: nginx
      state: Installed
  - name: playing the installing status
    ansible.builtin.debug:
      msg: " here we go the installing the nginx"

