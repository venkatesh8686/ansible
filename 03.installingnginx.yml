- name: Installing nginx
  hosts: web
  tasks: 
  - name: install nginx
    ansible.builtin.package:
      name: nginx
      state: present
  - name: Showing the status
    ansible.builtin.debug:
      msg: "The nginx installed"
  

